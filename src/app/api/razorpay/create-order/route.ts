import { NextResponse } from "next/server";

export async function POST() {
  // Razorpay order creation — implemented in Phase 6
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
